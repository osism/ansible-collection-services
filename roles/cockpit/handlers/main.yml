---
- name: Restart cockpit.socket service
  become: true
  ansible.builtin.systemd:
    state: restarted
    daemon_reload: true
    name: cockpit.socket

- name: Restart cockpit service
  become: true
  ansible.builtin.systemd:
    state: restarted
    daemon_reload: true
    name: cockpit
