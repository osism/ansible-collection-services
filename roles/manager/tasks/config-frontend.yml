---
- name: Copy frontend environment file
  ansible.builtin.template:
    src: "env/frontend.env.j2"
    dest: "{{ manager_configuration_directory }}/frontend.env"
    mode: 0640
    owner: "{{ operator_user }}"
    group: "{{ operator_group }}"
